<!DOCTYPE html>
<html>
<head>
  <title>TwitchClone</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="left-sidebar">
    <%= flash[:error] %>
    <%= current_user.username if !!current_user %>
    <%= button_to("Log Out!", session_url, method: :delete) if !!current_user %>

    <% if !!current_user %>
      <h3>Follows</h3>
      <ul>
      <% @user_follows.each do |follow| %>
        <div class="follow-area">
          <a href="<%= page_show_url(follow.username) %>">
            <%= "<img style='border:3px solid black;' src=#{Page.find_by_user_id(follow.id).logo} height=30 width=30>".html_safe %>
            <div class="follow-name">
            <li title="<%= h(follow.short_bio) %>"><%= h(follow.username << " " << follow.status.to_s) %></li>
            </div>
          </a>
        </div>
      <% end %>
      </ul>
      <h3>Followers</h3>
      <ul>
      <% @user_followers.each do |follower| %>
        <div class="follow-area">
          <a href="<%= page_show_url(follower.username) %>">
            <%= "<img style='border:3px solid black;' src=#{Page.find_by_user_id(follower.id).logo} height=30 width=30>".html_safe %>
            <div class="follow-name">
              <li title="<%= h(follower.short_bio) %>"><%= h(follower.username << " " << follower.status.to_s) %></li>
            </div>
          </a>
        </div>
      <% end %>
      </ul>
      <h3>Links</h3>
      <ul>
        <li><a href="<%= user_show_url(current_user.username) %>">My Profile</a></li>
        <li><a href="<%= page_show_url(current_user.username) %>">My Page</a></li>
        <li><a href="<%= videos_url(current_user.username) %>">My videos</a></li>
      </ul>
    <% else #no current user!! %>
      <%= render "partials/new_session" %>
      <%= render "partials/new_user" %>
    <% end %>
  </div>

  <div class="main-content">
    <%= yield %>
    <%#= @streams_info %>
  </div>

  <div class="right-sidebar"></div>

</body>
</html>
